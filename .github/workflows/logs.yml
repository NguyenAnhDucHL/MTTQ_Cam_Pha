name: Get Logs
on: workflow_dispatch
jobs:
  get-logs:
    runs-on: ubuntu-latest
    steps:
      - name: Execute remote ssh commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VNPT_HOST }}
          username: ${{ secrets.VNPT_USER }}
          password: ${{ secrets.VNPT_PASS }}
          script: |
            cd /root/MTTQ_Cam_Pha/Cong_MTTQ_Cam_Pha_Quan_Tri_3.0
            echo "=== Checking Docker Nginx error logs ==="
            docker-compose logs --tail=200 frontend || true
            echo "=== Checking backend logs again ==="
            docker-compose logs --tail=200 backend || true
